name: newsus
description: "Submission Belajar Membuat Aplikasi Flutter untuk Pemula."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flex_color_scheme: ^7.3.1 
  google_fonts: ^6.1.0
  flutter_dotenv: ^5.1.0 
  dio: ^5.4.3+1
  intl: ^0.19.0
  url_launcher: ^6.2.6


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/
